#include "main.h"
#include <stdio.h>

/**
 * _isupper - a function to check the uppercase alphas
 * @c: an integer parameter
 * Return: something but success
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
